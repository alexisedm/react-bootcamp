import React from "react"
import ReactDOM from "react-dom"
import App from "./App"
//use children you can do alot of  simple things and some props
 
//if you want to always ensure structure always a property component with props  
ReactDOM.render(<App/>, document.getElementById("root"))
